var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};

// src/Disclosure.tsx
import * as React2 from "react";
import { useDisclosurePrimitiveState } from "@twilio-paste/disclosure-primitive";
import { Box, safelySpreadBoxProps } from "@twilio-paste/box";
import { Card } from "@twilio-paste/card";

// src/DisclosureContext.tsx
import * as React from "react";
var DisclosureContext = React.createContext({});

// src/Disclosure.tsx
var Disclosure = React2.forwardRef((_a, ref) => {
  var _b = _a, { children, element = "DISCLOSURE", variant = "default", state } = _b, props = __objRest(_b, ["children", "element", "variant", "state"]);
  const disclosure = state || useDisclosurePrimitiveState(__spreadValues({ animated: true }, props));
  const disclosureContext = {
    disclosure,
    variant
  };
  if (variant === "contained") {
    return /* @__PURE__ */ React2.createElement(DisclosureContext.Provider, {
      value: disclosureContext
    }, /* @__PURE__ */ React2.createElement(Card, __spreadProps(__spreadValues({}, props), {
      variant,
      element,
      padding: "space0",
      ref
    }), children));
  }
  return /* @__PURE__ */ React2.createElement(DisclosureContext.Provider, {
    value: disclosureContext
  }, /* @__PURE__ */ React2.createElement(Box, __spreadProps(__spreadValues({}, safelySpreadBoxProps(props)), {
    variant,
    element,
    ref
  }), children));
});
Disclosure.displayName = "Disclosure";
if (false) {
  Disclosure.propTypes = DisclosurePropTypes;
}

// src/DisclosureHeading.tsx
import * as React4 from "react";
import { useTheme } from "@twilio-paste/theme";
import { Box as Box2, safelySpreadBoxProps as safelySpreadBoxProps2 } from "@twilio-paste/box";
import { Heading } from "@twilio-paste/heading";
import { ChevronDisclosureExpandedIcon } from "@twilio-paste/icons/esm/ChevronDisclosureExpandedIcon";
import { DisclosurePrimitive } from "@twilio-paste/disclosure-primitive";

// src/utils.ts
import * as React3 from "react";
var getIconHoverStyles = (isHovered, variant, ariaExpanded, ariaDisabled, space) => {
  if (!ariaDisabled) {
    let slideDistance = space.space20;
    if (variant === "heading40" || variant === "heading50" || variant === "heading60") {
      slideDistance = space.space10;
    }
    return {
      color: isHovered ? "colorText" : "colorTextIcon",
      transform: `${isHovered ? `translateX(${slideDistance})` : `translateX(0)`} ${ariaExpanded ? "rotate(0deg)" : "rotate(-90deg)"}`,
      transition: "transform 200ms ease-out"
    };
  }
  return {
    color: "colorTextIcon"
  };
};
var useHover = (elementRef) => {
  const [value, setValue] = React3.useState(false);
  const handleMouseOver = () => setValue(true);
  const handleMouseOut = () => setValue(false);
  React3.useEffect(() => {
    const node = elementRef == null ? void 0 : elementRef.current;
    if (node) {
      node.addEventListener("mouseover", handleMouseOver);
      node.addEventListener("mouseout", handleMouseOut);
      return () => {
        node.removeEventListener("mouseover", handleMouseOver);
        node.removeEventListener("mouseout", handleMouseOut);
      };
    }
  }, [elementRef]);
  return value;
};

// src/constants.ts
var IconSizeFromHeading = {
  heading10: "sizeIcon90",
  heading20: "sizeIcon70",
  heading30: "sizeIcon60",
  heading40: "sizeIcon40",
  heading50: "sizeIcon30",
  heading60: "sizeIcon20"
};

// src/DisclosureHeading.tsx
var StyledDisclosureHeading = React4.forwardRef((_a, ref) => {
  var _b = _a, { children, element, marginBottom, renderAs, disclosureVariant, customDisabled, customFocusable, variant } = _b, props = __objRest(_b, ["children", "element", "marginBottom", "renderAs", "disclosureVariant", "customDisabled", "customFocusable", "variant"]);
  const theme = useTheme();
  const hoverRef = React4.useRef(null);
  const isHovered = useHover(hoverRef);
  let bottomBorderRadius = "borderRadius20";
  if (disclosureVariant === "contained") {
    bottomBorderRadius = "borderRadius10";
  }
  if (disclosureVariant === "contained" && props["aria-expanded"]) {
    bottomBorderRadius = "borderRadius0";
  }
  return /* @__PURE__ */ React4.createElement(Heading, {
    as: renderAs,
    marginBottom: "space0",
    variant,
    ref
  }, /* @__PURE__ */ React4.createElement(Box2, __spreadProps(__spreadValues({}, safelySpreadBoxProps2(props)), {
    as: "div",
    backgroundColor: props["aria-expanded"] ? "colorBackground" : "colorBackgroundBody",
    borderRadius: disclosureVariant === "contained" ? "borderRadius10" : "borderRadius20",
    borderBottomLeftRadius: bottomBorderRadius,
    borderBottomRightRadius: bottomBorderRadius,
    cursor: "pointer",
    display: "flex",
    element,
    outline: "none",
    padding: "space30",
    position: "relative",
    ref: hoverRef,
    role: "button",
    zIndex: "zIndex10",
    transition: "background-color 100ms ease-out",
    _hover: {
      backgroundColor: "colorBackgroundStrong"
    },
    _focus: {
      boxShadow: "shadowFocus"
    },
    _disabled: {
      backgroundColor: props["aria-expanded"] ? "colorBackground" : "colorBackgroundBody",
      color: "colorTextWeak",
      cursor: "not-allowed"
    }
  }), /* @__PURE__ */ React4.createElement(Box2, __spreadValues({
    as: "span",
    element: `${element}_ICON`,
    display: "flex",
    height: IconSizeFromHeading[variant] || "sizeIcon20",
    width: IconSizeFromHeading[variant] || "sizeIcon20"
  }, getIconHoverStyles(isHovered, variant, props[`aria-expanded`], props[`aria-disabled`], theme.space)), /* @__PURE__ */ React4.createElement(ChevronDisclosureExpandedIcon, {
    color: "inherit",
    decorative: true,
    size: IconSizeFromHeading[variant] || "sizeIcon20"
  })), children));
});
StyledDisclosureHeading.displayName = "StyledDisclosureHeading";
var DisclosureHeading = (_a) => {
  var _b = _a, {
    children,
    as,
    element = "DISCLOSURE_HEADING",
    disabled,
    focusable
  } = _b, props = __objRest(_b, [
    "children",
    "as",
    "element",
    "disabled",
    "focusable"
  ]);
  const { disclosure, variant } = React4.useContext(DisclosureContext);
  return /* @__PURE__ */ React4.createElement(DisclosurePrimitive, __spreadProps(__spreadValues(__spreadValues({}, disclosure), props), {
    as: StyledDisclosureHeading,
    customDisabled: disabled,
    customFocusable: focusable,
    disabled,
    disclosureVariant: variant,
    element,
    focusable,
    renderAs: as
  }), children);
};
DisclosureHeading.displayName = "DisclosureHeading";
if (false) {
  DisclosureHeading.propTypes = HeadingPropTypes;
}

// src/DisclosureContent.tsx
import * as React5 from "react";
import { useSpring, animated } from "@twilio-paste/animation-library";
import { Box as Box3, safelySpreadBoxProps as safelySpreadBoxProps3 } from "@twilio-paste/box";
import { DisclosurePrimitiveContent } from "@twilio-paste/disclosure-primitive";
var AnimatedDisclosureContent = animated(Box3);
var DisclosureContent = React5.forwardRef((_a, ref) => {
  var _b = _a, { children, element = "DISCLOSURE_CONTENT", visible } = _b, props = __objRest(_b, ["children", "element", "visible"]);
  const { disclosure } = React5.useContext(DisclosureContext);
  const { opacity } = useSpring({
    opacity: disclosure.visible ? 1 : 0,
    onRest: disclosure.stopAnimation,
    config: {
      mass: 1,
      tension: 150,
      friction: 20,
      duration: 100
    }
  });
  return /* @__PURE__ */ React5.createElement(DisclosurePrimitiveContent, __spreadProps(__spreadValues(__spreadValues({}, disclosure), safelySpreadBoxProps3(props)), {
    as: AnimatedDisclosureContent,
    element,
    backgroundColor: "colorBackgroundBody",
    padding: "space50",
    ref,
    style: {
      opacity
    }
  }), children);
});
DisclosureContent.displayName = "DisclosureContent";
if (false) {
  DisclosureContent.propTypes = DisclosureContentPropTypes;
}
export {
  AnimatedDisclosureContent,
  Disclosure,
  DisclosureContent,
  DisclosureHeading,
  useDisclosurePrimitiveState as useDisclosureState
};
